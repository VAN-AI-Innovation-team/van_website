{
  "name": "van-official-website",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "dev:share": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "qr": "node scripts/generate-qr.mjs",
    "lint": "oxlint astro.config.mjs scripts src/data src/pages",
    "preview": "astro preview",
    "check": "npm run lint && npm run build",
    "preview:html": "node scripts/build-standalone-preview.mjs"
  },
  "dependencies": {
    "astro": "7.2.6"
  },
  "devDependencies": {
    "oxlint": "^1.75.0",
    "qrcode": "^1.5.4"
  }
}
